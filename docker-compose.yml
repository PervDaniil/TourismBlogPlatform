version: '2.30.3'

services:
  db:
    image: mongo
    restart: always
    volumes:
      - "mongodb:/db/data"
    ports:
      - "27017:27017"

  backend:
    build: ./backend/app/
    volumes:
      - "./backend/app/:/app"
    ports:
      - "8000:8000"
    depends_on:
      - db

  frontend:
    build: ./frontend/app/
    volumes:
      - "./frontend/app:/app"
    ports:
      - "3000:3000"
    depends_on:
      - backend

  nginx:
    image: nginx
    volumes:
      - "./nginx/nginx.conf:/etc/nginx/nginx.conf"
      - "./backend/app/media:/app/media"
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend


volumes:
  mongodb:
